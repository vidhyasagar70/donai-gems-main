// tailwind.config.ts
import type { Config } from "tailwindcss";

const config: Config = {
    // You might have other theme extensions here, but not keyframes/animation for v4
    // theme: {
    //   extend: {
    //     // ... other extensions
    //   },
    // },
    plugins: [],
};

export default config;
